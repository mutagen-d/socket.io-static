const EVENTS = {
  READ_FILE: '$fs.readFile',
  WRITE_FILE: '$fs.writeFile',
  STAT: '$fs.stat',
  READDIR: '$fs.readdir',
  EXISTS: '$fs.exists',
  DEL: '$fs.del',
  STRUCT: '$fs.struct',
  MKDIR: '$fs.mkdir',
}

module.exports = EVENTS;